// During testing

// Mock the actual method you're using in the controller
// const mockSiteDetail = jest.fn().mockResolvedValue({
//   status: 'success',
//   message: 'Site Details Fetched'
// });

// Replace the actual service method with the mock
// jest.spyOn(siteDetailService, 'get').mockImplementation(mockSiteDetail);